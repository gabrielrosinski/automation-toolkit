apiVersion: v1
kind: Service
metadata:
  name: automation-toolkit
  namespace: default
  labels:
    app: automation-toolkit
spec:
  type: NodePort
  selector:
    app: automation-toolkit
  ports:
  - port: 80
    targetPort: 8080  # Container runs on non-privileged port
    protocol: TCP
    name: http
