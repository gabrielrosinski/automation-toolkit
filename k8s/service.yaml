apiVersion: v1
kind: Service
metadata:
  name: automation-toolkit
  namespace: default
  labels:
    app: automation-toolkit
spec:
  type: NodePort
  selector:
    app: automation-toolkit
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
